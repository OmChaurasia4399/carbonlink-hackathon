import { LevelQuiz } from '../LevelQuiz';
import { ThemeProvider } from '../ThemeProvider';

export default function LevelQuizExample() {
  return (
    <ThemeProvider>
      <LevelQuiz />
    </ThemeProvider>
  );
}
